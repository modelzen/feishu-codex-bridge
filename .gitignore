node_modules/
dist/
coverage/
*.log
*.tsbuildinfo

.env
.env.local
.env.*.local

.DS_Store
.idea/
.vscode/

# third-party reference clones (each has its own git history)
docs/references/

# agent tooling / local state
.agents/
.agent-teams/
.claude/
.lead-sessions.json
skills-lock.json
prototype/appserver-probe/schema/

# local test recordings (binary; keep repo clean for future open-source)
docs/tests/

# per-task review report subdirs (the reviewer/ INDEX files are tracked; the
# individual review-*/ folders stay local)
.plans/**/review-*/
