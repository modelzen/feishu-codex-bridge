# feishu-codex-bridge — Team Operations Guide

> Auto-generated by agent-teams-mcp-setup. lead 读 CLAUDE.md，codex worker 读 AGENTS.md（内容相同，改一起改）。

## 项目一句话
飞书/Lark ↔ 本机 Codex 桥。**项目=群=固定cwd、话题=session**。后端 codex `app-server`（每会话一独立进程，JSON-RPC over stdio），传输 `@larksuiteoapi/node-sdk` 长连接。设计/实现权威文档：`docs/design/feishu-codex-bridge-design.md`、`docs/design/implementation-plan.md`。

## 技术栈
TS + Node≥20 · tsup 构建 · vitest 测试 · commander CLI · `@larksuiteoapi/node-sdk`(`createLarkChannel` 长连接) · codex CLI 经 app-server。`npm run typecheck` / `npm run build` / `npm test`。

## 已实现（git log 可查）
M0 脚手架 · M1 群@bot→reply_in_thread 建话题→app-server 跑→流式 markdown 卡片（已真机验证）· M2 私聊管理台+项目注册表 · M6 watchdog+steer/queue+并发。

## Team-Lead Control Plane
- team-lead = 主对话（Claude Code），不是 worker。拥有需求对齐、任务拆解、phase gate、主 plan + CLAUDE.md/AGENTS.md 维护。
- **lead 自己写耦合的卡片 UI 核心**（card/*、bot/handle-message、新 card/dispatcher）；worker 做独立切片。
- 团队建好后不再 spawn 标准 subagent。

## Team Roster
| Name | Role | Adapter | Model | Cwd | 关键能力 |
|------|------|---------|-------|-----|---------|
| service-dev | Backend Dev | codex | default | 仓库根 | M8 launchd 后台服务 + cli service 命令 |
| test-dev | Backend Dev/Test | codex | default | 仓库根 | 纯模块 vitest 单测（test/） |
| reviewer | Code Reviewer | codex | default | 仓库根 | 只读 review lead 的卡片代码 |

## 派发协议（双轨）
1. 文件事实源：`.plans/feishu-codex-bridge/<agent>/task_plan.md`（lead 维护）。
2. 触发：`mcp__team-mode__send_message(team="feishu-codex-bridge", text="@<agent> ...")`。
worker 靠 `send_message` 才算"说话"，stdout 不可见。

## 冲突规避（关键）
- service-dev 唯一会碰的共享文件是 `src/cli/index.ts` 的 service 命令段——只动那段。
- test-dev 只在 `test/` 新建文件，只读 import src。
- lead 独占 `card/*`、`bot/handle-message.ts`、新 `card/dispatcher.ts`。
→ 文件级隔离，正常无冲突。

## Review Dimensions（reviewer 每次评分；任一 WEAK→不能 [OK]）
| # | 维度 | Weight | STRONG | WEAK |
|---|------|--------|--------|------|
| RD-1 | 稳定性/隔离 | high | 卡死/异常不扩散；watchdog→turn/interrupt→SIGKILL 正确；子进程回收 | 异常冒泡崩 bot；卡死无超时；孤儿进程 |
| RD-2 | 飞书 API 正确性 | high | reply_in_thread/卡片 patch/置顶 用对接口；错误有 catch+日志 | 用错 receive_id_type；patch 失败无处理 |
| RD-3 | 与设计一致 | medium | 符合 docs/design（项目=群=cwd、私聊=控制台、零斜杠卡片化） | 私聊跑 codex；斜杠堆砌 |
| RD-4 | 最小实现/窄修改 | medium | 改动对应任务；不过度抽象 | 顺手重构无关代码；过度抽象 |
叠标准 checklist：安全/质量/性能/doc-sync。审批：[OK]无CRITICAL/HIGH且无WEAK · [WARN]仅MEDIUM · [BLOCK]有CRITICAL/HIGH或任一WEAK。

## Commit 协议
按 group/phase 边界 commit，禁积压大 diff。message：`<type>(<scope>): <subject>` + body + `Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>`。lead 在边界 `git status` 检查，>5 文件/300 行未提交即提醒。

## 通讯快查
派任务 `send_message(team,"@<name> ...")` · 看 worker `worker_list(team)` · 复活 `worker_add(...,on_existing="reuse")`。

## Known Pitfalls
- **`onMessage`/卡片回调里禁止 `await` 整轮 codex run**：`@larksuiteoapi/node-sdk` 的 SafetyPipeline 按 **chatId** 串行排队**所有**事件（消息 + `card.action.trigger` 共用一条 FIFO，`queueEnabled` 默认 true）。一个话题群 = 一个 chatId，所以 handler 一旦 await 长 run，就占住整条队列 → 同群其他话题不响应、⏹ 终止按钮排在队尾点了没反应。必须 **detach**（`void withTrace(async …)` 后台跑，handler 立即返回）。同话题双启动用同步预占 `active[threadId]` 防护。

## Style Decisions
(空 — 用户表达 taste 偏好时记)
